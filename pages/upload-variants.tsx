import { Layout } from '../src/components/layout';
import { UploadVariants } from '../src/components/templates/uploadVariants';

const UploadVariantsPage = () => {
    return (
        <Layout>
            <UploadVariants/>
        </Layout>
    );
};

export default UploadVariantsPage;